// DO NOT EDIT!
//
declare module "aws-sdk" {
    export class {{api.name}} {
      constructor(options?: any);
{{#each m.operations}}
      {{{camelCase @key}}}({{#if this.input.shape}}params: {{this.input.shape}}, {{/if}}callback: (err: {{#each this.errors}}{{this.shape}}|{{/each}}any, data: {{#if this.output.shape}}{{this.output.shape}}|{{/if}}any) => void): void;
{{/each}}
    }

{{#each m.shapes}}
{{{dumpShape this}}}

{{/each}}
}
